# =============================================================================
# Event Planner – Environment Variables
# =============================================================================
# This file contains non-sensitive defaults and is committed to git.
# Create a .env.local file for your local secrets (never commit .env.local).
# =============================================================================

# -----------------------------------------------------------------------------
# Supabase (required for auth & data)
# -----------------------------------------------------------------------------
# From Supabase dashboard: Settings → API
NEXT_PUBLIC_SUPABASE_URL=https://nwgzdfpbqahqewkbynqx.supabase.co
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=

# -----------------------------------------------------------------------------
# Google Maps (optional – used in map/location UI)
# -----------------------------------------------------------------------------
NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=

# -----------------------------------------------------------------------------
# Site URL (for OAuth redirect)
# -----------------------------------------------------------------------------
# Leave empty - set in .env.local for local dev, or via Vercel for production
NEXT_PUBLIC_SITE_URL=

# -----------------------------------------------------------------------------
# Twilio / WhatsApp (optional – guest messaging)
# -----------------------------------------------------------------------------
# From Twilio console. Required only if using WhatsApp messaging in guests.
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_SENDER_ID=
