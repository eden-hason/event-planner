# =============================================================================
# Event Planner – Environment Variables
# =============================================================================
# Copy this file to .env.local and fill in your values.
# Variables prefixed with NEXT_PUBLIC_* are exposed to the browser.
# =============================================================================

# -----------------------------------------------------------------------------
# Supabase (required for auth & data)
# -----------------------------------------------------------------------------
# From Supabase dashboard: Settings → API
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=

# Service role key (server-only, used for public confirmation pages that bypass RLS)
# From Supabase dashboard: Settings → API → service_role key
SUPABASE_SERVICE_ROLE_KEY=

# -----------------------------------------------------------------------------
# Google Maps (optional – used in map/location UI)
# -----------------------------------------------------------------------------
NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=

# -----------------------------------------------------------------------------
# Site URL (optional – for OAuth redirect in production)
# -----------------------------------------------------------------------------
# Set in production; defaults to NEXT_PUBLIC_VERCEL_URL on Vercel or localhost
NEXT_PUBLIC_SITE_URL=

# -----------------------------------------------------------------------------
# Twilio / WhatsApp (optional – guest messaging)
# -----------------------------------------------------------------------------
# From Twilio console. Required only if using WhatsApp messaging in guests.
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_SENDER_ID=

# -----------------------------------------------------------------------------
# Meta WhatsApp Business API (optional – WhatsApp messaging)
# -----------------------------------------------------------------------------
# From Meta for Developers: https://developers.facebook.com/apps/
WHATSAPP_PHONE_NUMBER_ID=
WHATSAPP_ACCESS_TOKEN=
